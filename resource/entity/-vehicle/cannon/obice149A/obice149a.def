{game_entity
	(include "/properties/cannon.ext")
	{props "vehicle" "cannon" "wheeled" "vision_cannon_medium" "round_sandbag5" "muzzle_maximum"}

	{PatherID  "cannon.large"}
	("armor_heavy")

	{Volume "Gun_rot"
		{tags "gun"}
		{component "gun"}
	}
	
	("crew_wheeled_gun")
	{boarder
		{anm "gunner"
			{forward	{end "pose_m5_gunlayer" 10}}
		}
		{anm "commander"
			{forward	{end "pose_pdr25_charger" 10}}
		}
		{anm "driver1"
			{forward {end "cannon_pak40_pusher_left" 10}}
		}
		{anm "driver2"
			{forward {end "cannon_pak40_pusher_right" 10}}
		}
		{anm "seat1"
			{forward {end "pose_seat_cannon_left" 10}}
		}
		{anm "seat2"
			{forward {end "pose_seat_cannon_right" 10}}
		}
	}
	{sensor
		{visor "view-around"
			{bone "basis"}
		}
	}

	("wheel" args "wheelright")
	("wheel" args "wheelleft")

	{extender "inventory"
		{box
			{item "bullet149ita fg" 4}
		    {item "sandbag_kit2" 25}
;           {item "plant" 5}
		}
	}
	{Weaponry

		{place "gun"
			{weapon "149mm_35" filling "bullet149ita fg" 1}
			{Rotate}
			("abm_howitzer")
;			{StationaryFiring}
		}
	}  	
	
	{mass 8200}
	
	{Extension "obice149a.mdl"}
	(include "/properties/selection/cannon.inc"  scale(0.7))
 
		{extender "cannon"
			{animation
				{move	"cannon_pak40_pusher_left" "cannon_pak40_pusher_right"}
				{aim	"cannon_m5_gunlayer" ""}
				{fire	"Cannon_gunlayer_fire-alert" "cannon_pdr25_charger_fire"}
				{reload	"" "Cannon_pdr25_charger"}
			}
			{move {obstacle "close"}}
			{attack {obstacle "open"}}
		}

	{Chassis "track"
		("car_trace" fx("tracewheel05a") step(0.14) wheell("wlf") wheelr("wrf") bonel("FXtraceL1") boner("FXtraceR1"))
		("loco_verybig")
	}

	{bone "basis"
		{limits}
    		{speed 0.003}
    	}
		
	{bone "gun"
		{speed2 5}
	}
;	{bone "gun_rot"
;		{speed2 5}
;	}		
}
