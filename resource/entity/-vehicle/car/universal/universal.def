{game_entity
	(include "/properties/car.ext")
    (include "/properties/l_car_mobility.ext")
	(include "/properties/selection/vehicle.inc"  scale(1.1))

	{props "vehicle" "car" "wheeled" "truck" "vision_vehicle_minimum"}
	{able "emit_on_attack"}
	{targetClass "car" "fg bullet"}	
	("armor_medium" args 1.0)

	("crew_inside")
	("crew_human" place(driver)		bone(driver)	door(emit1) animation(driver))
	("board_animation"
		place(driver)
		board_begin(board_big-midcar_driver)
		board_end(seat_driver_car)
;		base(doorleft)
	)

	("wheel" args "wheelright1")
	("wheel" args "wheelleft1")
	("wheel" args "wheelright2")
	("wheel" args "wheelleft2")

	{mass 2100}
	{Chassis
		("car_trace" fx("tracks_sm") step(0.3) wheell("wlf") wheelr("wrf") bonel("FXtraceL2") boner("FXtraceR2"))
		("mobility"
			speed(60)
			weight(2.1)
			power(22)
			fuel(40)	
		)
	}

	{Extension "Universal.mdl"}
}
