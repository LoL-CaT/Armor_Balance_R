(include "\set\small.firearms.accuracy")

(define "recoveryTime"   
	{recoveryTime (/ 60 %r)}
)

(define "rechargeTime" 
    {rechargeTime (/ 60 %r)}
)

(define "speed"   
	{speed (* 0.1 %s)}  
)

(define "bolt_action"
	{ZeroingAccuracy 0.9 0.8}
)