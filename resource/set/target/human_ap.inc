{selector
	{unit "human" "ap"}

	{group
		{ignore {class airborne}}
		{ignore {class attacker}}
		{ignore {class fighter}}
		{sort {class tank}}
		{sort {class armoredcar}}
		{sort {class car}}
		{sort {attacking}}
		{sort distance}
    }
}